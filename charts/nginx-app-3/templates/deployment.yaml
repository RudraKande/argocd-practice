apiVersion: apps/v1
kind: Deployment

metadata:
  name: nginx-app-3

spec:
  replicas: {{ .Values.replicaCount }}

  selector:
    matchLabels:
      app: nginx-app-3

  template:
    metadata:
      labels:
        app: nginx-app-3

    spec:
      containers:
        - name: nginx
          image: nginx:latest

          ports:
            - containerPort: 80
